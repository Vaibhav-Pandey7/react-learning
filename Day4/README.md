we only write js expressions inside jsx   
expressions basically means any thing that produces a result  
we dont write statements(like let x=10;)   
React components are just JS functions, JSX is just a translator, and props(properties) are just automatically bundled object arguments.