JSX->JavaScript XML

Bundler->used to make the code production ready
Babel->used to convert JSX->React compatible code
but here we are usign vite that has its inbuilt esbuild that does the work of babel for us

